{
  "name": "@vocalendar/schemas",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsdown src/index.ts --format esm --platform neutral --out-dir dist --dts --clean",
    "check": "rslint --type-check . && oxfmt --check .",
    "test": "vitest run",
    "fix": "rslint --type-check --fix . && oxfmt ."
  },
  "dependencies": {
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@rslint/core": "^0.5.3",
    "oxfmt": "^0.52.0",
    "tsdown": "^0.22.0",
    "typescript": "~6.0.3",
    "vitest": "^4.0.15"
  }
}
